<UserControl
    x:Class="NativeUwp_ViewerApp_01.NumericValueEditor_UserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeUwp_ViewerApp_01"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="70"
    d:DesignWidth="160"
>

    <StackPanel Orientation="Vertical" Background="LightSeaGreen">

        <TextBox PlaceholderText="Position" Text="{x:Bind ViewModel.DisplayName, Mode=OneWay}" />

        <StackPanel Orientation="Horizontal" >
        
        <Slider 
            HorizontalAlignment="Left"
            Minimum="{x:Bind ViewModel.MinValue, Mode=OneWay}"  
            Maximum="{x:Bind ViewModel.MaxValue, Mode=OneWay}" 
            Value="{x:Bind ViewModel.CurrentValue, Mode=TwoWay}"
            Width="100" 
        />

        <TextBlock Margin="10,10,10,10" Text="0.4" />

        </StackPanel>

    </StackPanel>

</UserControl>
