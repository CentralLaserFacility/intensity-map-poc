<UserControl
    x:Class="NativeUwp_ViewerApp_01.ViewerPanel_UserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeUwp_ViewerApp_01"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="1200"
>

    <Grid ColumnSpacing="20" RowSpacing="20" Background="BlanchedAlmond">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <local:IntensityMapImageAndProfileGraphs_UserControl Grid.Row="0" Grid.Column="0" />

        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical">
          <local:ImagePresentationSettings_UserControl Height="300" Margin="20,20,20,10"/>
          <local:ProfileDisplaySettings_UserControl Height="200" Margin="20,10,20,10" />
          <StackPanel Orientation="Horizontal" Padding="10" Margin="20" >
            <Button Content="Settings" />
            <Button Content="Reset" />
          </StackPanel>
        </StackPanel>

        <Rectangle Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Fill="Green" />

    </Grid>

</UserControl>
