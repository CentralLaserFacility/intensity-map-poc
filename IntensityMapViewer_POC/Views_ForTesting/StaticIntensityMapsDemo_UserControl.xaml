<UserControl
    x:Class="Views_ForTesting.StaticIntensityMapsDemo_UserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Views_ForTesting"
    xmlns:uwpUtils="using:UwpUtilities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>

      <StackPanel Orientation="Vertical">

        <Button Command="{x:Bind ViewModel.MoveToNextStaticImage}">Click to cycle through examples</Button>

        <TextBlock Text="{x:Bind ViewModel.IntensityMapLabel, Mode=OneWay}" />

        <Image 
            x:Name="m_image" 
            Source="{x:Bind uwpUtils:BitmapHelpers.CreateWriteableBitmap(ViewModel.IntensityMap,ViewModel.ColourMapOption), Mode=OneWay}"
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Width="640"
            Height="480" 
            Margin="0,0,0,0" 
            Visibility="Visible"
        />

      </StackPanel>

    </Grid>
</UserControl>
